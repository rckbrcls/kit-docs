---
title: CLI
description: Use npx @polterware/pwa init to create or update manifest.json interactively.
---

The CLI provides an interactive way to create or update your web app manifest.

## Usage

### Create or update manifest.json

```bash
npx @polterware/pwa init
```

### Custom manifest path

```bash
npx @polterware/pwa init --manifest-path public/manifest.json
```
