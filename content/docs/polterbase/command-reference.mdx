---
title: Command Reference
description: Complete reference of Supabase CLI commands available through the Polterbase interactive board.
---

## Execution Model

Polterbase executes workflow commands as:

```bash
supabase <command> <extra-args> <global-flags>
```

The self-update action is the only built-in exception and runs one of:

```bash
npm install -g @polterware/polterbase@latest    # global
npm install -D @polterware/polterbase@latest     # repository
```

## Quick Start

| Command | Description |
|---|---|
| `bootstrap` | Bootstrap a Supabase project from a starter template |

## Local Development

| Command | Description |
|---|---|
| `db` | Manage Postgres databases |
| `gen` | Run code generation tools |
| `init` | Initialize a local project |
| `inspect` | Inspect Supabase project resources |
| `link` | Link local project to remote Supabase project |
| `login` | Authenticate with Supabase access token |
| `logout` | Remove local auth token |
| `migration` | Manage migration scripts |
| `seed` | Seed project from `supabase/config.toml` |
| `services` | Show local service versions |
| `start` | Start local Supabase containers |
| `status` | Show local container status |
| `stop` | Stop local Supabase containers |
| `test` | Run tests against local stack |
| `unlink` | Unlink local project |

## Management APIs

| Command | Description |
|---|---|
| `backups` | Manage project backups |
| `branches` | Manage preview branches |
| `config` | Manage project configuration |
| `domains` | Manage custom domains |
| `encryption` | Manage encryption settings |
| `functions` | Manage edge functions |
| `network-bans` | Manage network bans |
| `network-restrictions` | Manage network restrictions |
| `orgs` | Manage organizations |
| `postgres-config` | Manage Postgres configuration |
| `projects` | Manage Supabase projects |
| `secrets` | Manage project secrets |
| `snippets` | Manage SQL snippets |
| `ssl-enforcement` | Manage SSL enforcement |
| `sso` | Manage single sign-on |
| `storage` | Manage storage buckets |
| `vanity-subdomains` | Manage vanity subdomains |

## Additional Commands

| Command | Description |
|---|---|
| `completion` | Generate shell completion script |
| `help` | Show Supabase command help |

## Custom Command / Check Version

Use this mode for free-form args:

- `-v` to check the Supabase CLI version
- `status -o json` to get status as JSON
- `db pull` to pull remote schema
- `projects list` to list projects

## Global Flags

Available global flags in the interactive selector:

| Flag | Description |
|---|---|
| `--create-ticket` | Create a support ticket on error |
| `--debug` | Enable debug logs |
| `--experimental` | Enable experimental features |
| `--yes` | Auto-confirm prompts |
